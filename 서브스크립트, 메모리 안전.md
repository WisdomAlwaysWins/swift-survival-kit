#swift #subscript #optimization 

## 1. 서브스크립트 (Subscript)

`[]` 대괄호로 접근하는 것의 정체다. `array[0]`은 사실 Array에 정의된 서브스크립트 메서드를 호출하는 것이다. 직접 만들 수도 있다!

- [[Swift-프로퍼티-stored-computed-observer-lazy-type|연산 프로퍼티]]와 형태가 유사 (getter/setter)
- 인스턴스에 메모리 공간을 차지하지 않음 (메서드이므로)
- 파라미터 여러 개도 가능: `matrix[row, col]`

```swift
struct Scoreboard {
	private var scores: [String, Int] = [:]
	
	subscript(player: String) -> Int {
		get { scores[player] ?? 0 }
		set { scores[player] = newValue }
	}
}

var board = Scoreboard()
board["Alice"] = 95
board["Bob"] = 87
print(board["Alice"]) // 95
print(board["Unknown"]) // 0 (기본값)
```

---
## 2. 메모리 안전

같은 메모리 위치에 **동시에 읽기와 쓰기**가 일어나는 것을 방지한다. 특히 `inout` 파라미터에서 발생할 수 있다.

Apple 문서에서 명시하는 3가지 충돌 조건 (모두 만족해야 충돌):
1. 최소 하나는 **쓰기** 접근
2. 같은 메모리** 위치에 접근
3. 접근 시간이 **겹침**

```swift
var score = 10

// 🚩 같은 변수를 inout으로 두 번 전달 → 메모리 접근 충돌
swapAnything(&score, &score) // 컴파일 에러

// 구조체의 프로퍼티를 동시에 inout으로 전달
struct Point { var x: Int, y: Int }
var point = Point(x: 1, y: 2)
// swapAnything(&point.x, &point.y) // 컴파일 에러 (같은 struct의 다른 프로퍼티)
```

